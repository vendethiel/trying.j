*/~ >: i. 10

NB. */~ >: i. 10
NB.        i. 10 NB. 0..^10
NB.     >:       NB. Increment
NB. */           NB. Outer product
NB.  ~           NB. Reflexives. Makes a monad from a dyad by duplicating the argument
